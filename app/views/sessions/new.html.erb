<div class="flex items-center justify-center min-h-screen">
  <div class="card w-full max-w-md shadow-lg bg-base-100">
    <div class="card-body">
      <h1 class="text-2xl font-bold text-center mb-6">Login</h1>

      <%= form_with url: login_path, local: true do |f| %>
        <div class="form-control mb-4">
          <%= f.label :email, class: "label" %>
          <%= f.email_field :email, required: true, class: "input input-bordered w-full" %>
        </div>

        <div class="form-control mb-4">
          <%= f.label :password, class: "label" %>
          <%= f.password_field :password, required: true, class: "input input-bordered w-full" %>
        </div>

        <div class="form-control mt-6">
          <%= f.submit "Entrar", class: "btn btn-primary w-full" %>
        </div>
      <% end %>

      <p class="text-center mt-4">
        Não tem conta? 
        <%= link_to "Criar conta", signup_path, class: "link link-primary font-bold" %>
      </p>
    </div>
  </div>
</div>